apiVersion: v1
kind: Service
metadata:
  name: {{ include "davis.fullname" . }}
  labels:
    {{- include "davis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "davis.selectorLabels" . | nindent 4 }}
